# 2026-05-07

## Сессия 1
Тема: —

### Сделано
- Прочитал SOUL.md, MEMORY.md, GOALS.md, memory/README.md перед стартом
- Установил VPS сервер (159.194.231.38, Ubuntu 26.04 LTS): Node.js 20.20.2, Claude Code 2.1.132, VS Code CLI 1.119.0
- Создал пользователя `agent`, папки workspace/memory/knowledge/projects, .agent/bot, .claude/skills
- Скопировал на сервер DNA-файлы (CLAUDE.md, SOUL.md, MEMORY.md, GOALS.md), папки memory/, knowledge/, .claude/ (с plugins/superpowers и 4 скиллами)
- Симлинк /home/agent/CLAUDE.md → /home/agent/workspace/CLAUDE.md (единый мозг для бота и VS Code)
- Скачал файлы Telegram-бота и зависимости (npm install), создал /home/agent/.agent/.env с BOT_TOKEN

### Решения
- На Windows для автоматизации SSH без `expect`/`sshpass` использовал модуль PowerShell **Posh-SSH** (установлен в CurrentUser scope). Plink/scp висел на интерактивном host-key prompt
- Helper-скрипты в `$env:TEMP`: srv.ps1 (выполнение команд), srv-copy.ps1 (SCP)
- Папку .claude передавал tar.gz архивом (не SCP-папкой) — Posh-SSH 3.2.7 баг PercentComplete > 100 при больших структурах

### TODO на потом
- Проверить что VS Code Desktop видит туннель `jarvis-server` и открывает /home/agent/workspace/
- Если нужен фронт-офис на сервере: завести проекты в `/home/agent/projects/`

### Решения (продолжение)
- Авторизация Claude в VNC консоли Beget провалилась (OAuth code 95 символов вручную не вводится, копи-паст в VNC ненадёжен, code expired при попытке). **Сработавшее решение** — перенёс `~/.claude/.credentials.json` со своего локального ПК на сервер в `/home/agent/.claude/` (chmod 600, agent:agent). Подписка работает на обоих.
- VS Code `tunnel service install` упал из-за отсутствия dbus user-session под `agent`. Создал systemd unit `/etc/systemd/system/code-tunnel.service` вручную (System unit, не User), запускает `code tunnel --accept-server-license-terms --name jarvis-server` от agent. Активен.
- Telegram-бот: `agent-bot.service` active, auto-lock сработал на её `/start`
